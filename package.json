{
  "name": "shiv-security",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for Shiv Security — registered 2020, active since 2023",
  "scripts": {
    "dev": "live-server --port=8000 --open=index.html --watch=. --wait=200",
    "start": "npx http-server -p 8000",
    "start:serve": "npx serve -s . -l 8000",
    "open": "open http://localhost:8000"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
